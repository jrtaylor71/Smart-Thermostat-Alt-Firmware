[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

lib_deps =
    WiFi
    ESP Async WebServer
    Preferences
    Wire
    DHT sensor library
    TFT_eSPI
    HTTPClient
    PubSubClient
    EEPROM

monitor_speed = 115200
